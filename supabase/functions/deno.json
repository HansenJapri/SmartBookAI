{
  "//": "Konfigurasi Deno untuk Edge Function. Ada di sini (bukan root repo) supaya Deno berhenti di folder ini dan tidak ikut membaca package.json milik frontend — tanpa ini, type-check gagal mencari 'npm:@types/node' yang dirujuk tipe @supabase/supabase-js dari esm.sh, padahal node_modules repo memang bukan untuk Deno.",
  "nodeModulesDir": "none",
  "lock": false,
  "tasks": {
    "//test": "--allow-read dibutuhkan invariant_test.ts, yang menelusuri folder Edge Function untuk memastikan tiap fungsi memakai rate-limiter. Tanpa flag ini seluruh berkas itu gagal dengan NotCapable sebelum satu assert pun jalan.",
    "test": "deno test --allow-import --allow-read _shared/ai/"
  },
  "compilerOptions": {
    "lib": ["deno.window", "dom"],
    "strict": true
  }
}
